[project]
name = "exam-tutor-rag-core"
version = "0.1.0"
description = "RAG core: vector store + retrieval + prompt building"
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.27",
  "psycopg[binary]>=3.2.0",
  "psycopg-pool>=3.2.2",
  "pgvector>=0.4.1",
  "pydantic>=2.6",
  "exam-tutor-common",
]

[tool.ruff]
line-length = 100
target-version = "py310"
