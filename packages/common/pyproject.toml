[project]
name = "exam-tutor-common"
version = "0.1.0"
description = "Shared schemas and utilities for Exam Tutor MVP"
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
  "python-json-logger>=2.0.7",
  "uuid6>=2024.7.10",
]

[tool.ruff]
line-length = 100
target-version = "py310"
